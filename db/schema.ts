// Define your tables here. Drizzle Kit reads this file to generate
// migrations (`bun run db:generate`) applied with `bun run db:migrate`.
// Example:
// import { pgTable, serial, text } from "drizzle-orm/pg-core"
//
// export const demoUsers = pgTable("demo_users", {
//   id: serial("id").primaryKey(),
//   name: text("name"),
// })

export {}
